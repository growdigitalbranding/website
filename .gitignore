/node_modules
/.pnp
.pnp.js
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

# Duplicate skill copies the skills CLI writes for the Eve agent target.
# The canonical copies live in .agents/skills (symlinked into .claude/skills).
/agent/

# Installed skill packages. 11MB across 290 files, none of which the site
# builds from or serves: they are tooling for whoever is editing the repo,
# not part of the application. Tracking them slowed every clone and every
# deploy pull for nothing. The files stay on disk; git just stops carrying
# them.
/.agents/
